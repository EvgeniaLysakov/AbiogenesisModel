<Window
    x:Class="AbiogenesisModel.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:app="clr-namespace:AbiogenesisModel.App"
    xmlns:chrt="clr-namespace:AbiogenesisModel.App.Chart"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=app:MainViewModel}"
    Title="Abiogenesis Simulation"
    Width="1400"
    Height="900"
    MinWidth="1100"
    MinHeight="700">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="180"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Grid.ColumnSpan="3" Margin="0,0,0,12">
            <TextBlock
                Text="{Binding Status, StringFormat=Status: {0}}"
                FontSize="14"
                VerticalAlignment="Center"
                HorizontalAlignment="Right"
                DockPanel.Dock="Right"/>
        </DockPanel>

        <!-- Left control panel -->
        <Border
            Grid.Row="1"
            Grid.Column="0"
            Grid.RowSpan="2"
            BorderBrush="#DDD"
            BorderThickness="1"
            CornerRadius="6"
            Padding="12">

            <StackPanel>
                <TextBlock Text="Run Configuration" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>

                <TextBlock Text="Configuration files"/>
                <Button Content="Show..." Command="{Binding ShowConfigurationFilesCommand}" IsEnabled="{Binding LoadCommandEnabled}" Margin="0,0,6,6" Height="32"/>

                <TextBlock Text="Max ticks"/>
                <TextBox Text="{Binding MaxTicks, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding RunCommandEnabled}" Margin="0,4,0,12"/>

                <TextBlock Text="Report every N ticks"/>
                <TextBox Text="{Binding ReportEveryTicks, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding RunCommandEnabled}" Margin="0,4,0,20"/>

                <UniformGrid Columns="2" Rows="2" Margin="0,0,0,16">
                    <Button Content="Load" Command="{Binding LoadCommand}" IsEnabled="{Binding LoadCommandEnabled}" Margin="0,0,6,6" Height="32"/>
                    <Button Content="Run" Command="{Binding RunCommand}" IsEnabled="{Binding RunCommandEnabled}" Margin="6,0,0,6" Height="32"/>
                    <Button Content="Pause" Command="{Binding PauseCommand}" IsEnabled="{Binding PauseCommandEnabled}" Margin="0,6,6,0" Height="32"/>
                    <Button Content="Stop" Command="{Binding StopCommand}" IsEnabled="{Binding StopCommandEnabled}" Margin="6,6,0,0" Height="32"/>
                </UniformGrid>

                <Button Content="Save Snapshot" Command="{Binding SaveSnapshotCommand}" IsEnabled="{Binding SaveSnapshotCommandEnabled}" Height="32" Margin="0,0,0,20"/>

                <Separator Margin="0,8,0,12"/>

                <TextBlock Text="Current Run" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Tick:" Margin="0,0,12,6"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentTick, Mode=OneWay}"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Speed:" Margin="0,0,12,6"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TicksPerSecond, Mode=OneWay, StringFormat={}{0:F0} ticks/sec}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Elapsed:" Margin="0,0,12,6"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Elapsed, Mode=OneWay}"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Progress:" Margin="0,0,12,6"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Progress, Mode=OneWay, StringFormat={}{0:F0}%}"/>
                </Grid>

                <ProgressBar Height="18" Minimum="0" Maximum="100" Value="{Binding Progress, Mode=OneWay}" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main content area -->
        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="220"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top statistics cards -->
            <UniformGrid Grid.Row="0" Columns="4">
                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Molecules" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Molecules}" FontSize="32" Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>

                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12" Margin="8,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Strands" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Strands}" FontSize="32" Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>

                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12" Margin="8,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Nucleotides" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Nucleotides}" FontSize="32" Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>

                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Events / sec" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding EventsPerSecond}" FontSize="32" Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Main visualization tabs -->
            <TabControl Grid.Row="2">
                <TabItem Header="Population">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
                            <chrt:ChartWrapper ChartViewModel="{Binding StrandLengthDistributionChart}"/>
                        </Border>

                        <Border Grid.Column="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
                            <chrt:ChartWrapper ChartViewModel="{Binding StrandTotalChart}"/>
                        </Border>
                    </Grid>
                </TabItem>

                <TabItem Header="Events">
                    <Grid Margin="12">
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
                            <StackPanel>
                                <TextBlock Text="Event counters" FontWeight="SemiBold" Margin="0,0,0,12"/>

                                <DataGrid
                                    ItemsSource="{Binding EventCounters}"
                                    AutoGenerateColumns="False"
                                    CanUserAddRows="False"
                                    IsReadOnly="True"
                                    Height="300">

                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Event" Binding="{Binding EventName}" Width="*" />
                                        <DataGridTextColumn Header="Attempts" Binding="{Binding Attempts}" Width="120" />
                                        <DataGridTextColumn Header="Successes" Binding="{Binding Successes}" Width="120" />
                                        <DataGridTextColumn Header="No effect" Binding="{Binding NoEffect}" Width="120" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>

                <TabItem Header="Environment">
                    <Grid Margin="12">
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
                            <StackPanel>
                                <TextBlock Text="Environment over time" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                <TextBlock Text="[Temperature / pH / UV chart placeholder]" Foreground="#777"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>

                <TabItem Header="Snapshots">
                    <Grid Margin="12">
                        <TextBlock Text="[Snapshot browser placeholder]" Foreground="#777"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- Log panel -->
        <Border
            Grid.Row="2"
            Grid.Column="2"
            BorderBrush="#DDD"
            BorderThickness="1"
            CornerRadius="6"
            Padding="12"
            Margin="0,12,0,0">

            <DockPanel>
                <TextBlock
                    Text="Log"
                    FontWeight="SemiBold"
                    DockPanel.Dock="Top"
                    Margin="0,0,0,8"/>

                <ListBox ItemsSource="{Binding LogMessages}"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>

